const original = [15, 7, 13, 17, 26, 34, 18, 245];
const filtrado = [];
for (number of original) {
    if ((number > 10 && number < 20) || (number > 100)) {
        filtrado.push(number);
    }
}
console.log(filtrado);